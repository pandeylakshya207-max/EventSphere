# Copy to .env and fill in real values. Never commit .env itself.

# Required: secret used to sign JWT session tokens. Generate a real random
# value for production, e.g.: openssl rand -base64 32
JWT_SECRET=changeme-generate-a-real-random-secret

PORT=4000

# Optional: override the SQLite database file location.
# DB_PATH=./eventsphere.db
